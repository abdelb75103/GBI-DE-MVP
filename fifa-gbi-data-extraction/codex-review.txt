findings:
  - severity: high
    file: src/components/note-composer.tsx
    line: 66
    comment: |
      The new auto-save uses `saveNote()` both on blur and on submit. Clicking the Save button blurs the textarea first, so the request is sent twice. Consider removing the blur save or guarding with a flag/`isPending` so each note is posted only once.
  - severity: high
    file: src/app/api/papers/[paperId]/notes/[noteId]/route.ts
    line: 7
    comment: |
      The DELETE endpoint doesn’t check `readActiveProfileSession()` or verify the note belongs to the given paper/profile. Any caller who knows a note UUID can delete it. Align with the POST handler by enforcing auth and ensuring `paper_id` matches the route parameter before deleting.
