interface:
  display_name: "GBI Live Extraction"
  short_description: "Terminal-first live paper extraction and QA workflow"
  default_prompt: "Use $gbi-live-extraction to stage, review, apply, and high-reasoning QA-check a FIFA GBI paper or batch."

policy:
  allow_implicit_invocation: true
